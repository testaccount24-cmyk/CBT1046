*----------------------------------------------------------------------*
|                                                                      |
|                        I M P O R T A N T                             |
|                                                                      |
*----------------------------------------------------------------------*

Once this dataset has been RECEIVEd you will need to execute the UNPACK
member. This will unpack all the TRANSMITTED datasets.

You do this by entering the following command on a TSO command line:

 TSO EXEC 'install.GENPREP.PDS(UNPACK)'

Reply to the prompts and when the exec is complete read the $$INSTAL
member to finish installing the facility.

